function showText(text) {
    alert(text)
} // Function Declaration 

showText()

var showT = function(text) {
    alert(text)
} // Function Expression

showT()

// Дикларейшон можно вызвать везде внутри 
// блока где объявлена функция.

// Експрешон можно вызвать только после 
// объявления в любой части документа.